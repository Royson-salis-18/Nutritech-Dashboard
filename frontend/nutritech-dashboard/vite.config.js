import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  // Built dashboard is served by Flask at /dashboard
  base: '/dashboard/',
})
